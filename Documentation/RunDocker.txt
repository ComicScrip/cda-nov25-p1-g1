TOUT supprimer:

docker builder prune -a -f
docker image prune -a -f
docker system prune -a --volumes -f


Demarrer:

docker-compose down
docker-compose build --no-cache backend (éviter le cache corrompu??)
docker-compose up backend

docker-compose exec backend npm run resetDB (permet d'executer les scrip json du package dans docker lui meme)
(ou docker-compose exec backend resetDB)

docker-compose build --no-cache frontend
docker-compose up frontend

docker-compose exec frontend npm run codegen
docker-compose exec frontend npm run build
